declare namespace libjs { }
declare var module: any
if (typeof module !== 'undefined' && module.exports) {
  module.exports = libjs
} else {
  (<any>window).libjs = libjs
  document.addEventListener('DOMContentLoaded', e => libjs.domReady())
}